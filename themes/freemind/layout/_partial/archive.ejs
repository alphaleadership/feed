
<%
  var title = '';
  if (page.archive) {
    if (page.year) title = page.year + (page.month ? '/' + page.month : '');
    else title = __('archive_a');
  }

  // Utiliser les données de breaches.json
  var posts = site.data.breaches ? site.data.breaches.breaches : [];
  var archiveData = {
      years: {},
      months: ['Janvier', 'Février', 'Mars', 'Avril', 'Mai', 'Juin', 'Juillet', 'Août', 'Septembre', 'Octobre', 'Novembre', 'Décembre']
  };
     const invalidcategory=["hygiène numérique","sécurité","cybersécurité","cybercriminalité","cyberguerre"]
  // Ajouter l'index à chaque post et trier
  var filteredPosts = posts.filter((item)=>{
          return item && item.Name && !item.Name.includes('cve')
        }).filter((item)=>{
          return !item.IsRetired
        }).filter((c)=>{
          if(c && c.Name && c.Name.toLowerCase().includes("cve-")){
            return false
          }
          
              return true
        }).map((item, index) => {
    //item.index = index; // Ajouter l'index pour le lien
    return item;
  }).sort((a, b) => new Date(b.BreachDate) - new Date(a.BreachDate))
    

  filteredPosts.forEach(function(item) {
      var date = new Date(item.BreachDate);
      var year = date.getFullYear();
      var month = date.getMonth();
      
      if (!archiveData.years[year]) {
         archiveData.years[year] = {};
      }
      if (!archiveData.years[year][month]) {
         archiveData.years[year][month] = [];
      }
      archiveData.years[year][month].push(item);
   });

   // Calculer les fuites ajoutées aujourd'hui
   var todayStr = new Date().toISOString().split('T')[0];
   var addedTodayCount = filteredPosts.filter(function(item) {
     return (new Date(item.AddedDate || item.BreachDate).toISOString().split('T')[0]) === todayStr;
   }).length;

   // Calculer les statistiques de dépassement pour toutes les années à l'avance
   var yearSurpassInfo = {};
   var totalYearsCount = Object.keys(archiveData.years).length;
   var totalBreachesCount = filteredPosts.length;
   var avgBreachesPerYear = totalBreachesCount / totalYearsCount;
   
   Object.keys(archiveData.years).sort((a, b) => a - b).forEach(function(year) {
      var yearCount = Object.keys(archiveData.years[year]).reduce(function(total, monthKey) { 
        return total + archiveData.years[year][monthKey].length; 
      }, 0);
      
      var prevYear = parseInt(year) - 1;
      if (archiveData.years[prevYear]) {
        var prevYearCount = Object.keys(archiveData.years[prevYear]).reduce(function(total, monthKey) {
          return total + archiveData.years[prevYear][monthKey].length;
        }, 0);
        
        yearSurpassInfo[year] = {
          diff: yearCount - prevYearCount,
          percentage: (((yearCount - prevYearCount) / prevYearCount * 100).toFixed(1)),
          prevYearCount: prevYearCount,
          prevYear: prevYear
        };
        yearSurpassInfo[year].percentage = (yearSurpassInfo[year].diff >= 0 ? "+" : "") + yearSurpassInfo[year].percentage + "%";
        
        if (yearCount >= prevYearCount && prevYearCount > 0) {
           var allYearItems = [];
           Object.keys(archiveData.years[year]).forEach(function(monthKey) {
             allYearItems = allYearItems.concat(archiveData.years[year][monthKey]);
           });
           allYearItems.sort((a, b) => new Date(a.BreachDate) - new Date(b.BreachDate));
           var surpassDate = new Date(allYearItems[prevYearCount - 1].BreachDate);
           yearSurpassInfo[year].date = surpassDate;
           yearSurpassInfo[year].days = Math.ceil((surpassDate - new Date(year, 0, 1)) / (1000 * 60 * 60 * 24));
        }
      }
   });
%>

<!-- title -->
<div class="page-header <% if (theme.inverse){ %>page-header-inverse<% } %>">
  <h1 class="archive-title title <% if (theme.inverse){ %>title-inverse<% } %>">
    <%= title %>
    <% if (addedTodayCount > 0) { %>
      <span class="badge badge-success" style="font-size: 0.4em; vertical-align: middle; background-color: #28a745; margin-left: 10px; padding: 5px 10px;">
        <%= addedTodayCount %> NOUVELLE<%= addedTodayCount > 1 ? 'S' : '' %> AUJOURD'HUI
      </span>
    <% } %>
  </h1>
</div>

<% if (!filteredPosts || filteredPosts.length === 0) { %>
  <div class="alert alert-info" role="alert">
    Aucune fuite de données trouvée.
  </div>
<% } else { %>
  <div class="row page">
    <div class="<% if (theme.widgets.length) { %>col-md-9<% } else { %>col-md-12<% } %>">
      
      <!-- Navigation par année (en haut) -->
      <div class="archive-year-nav" style="margin-bottom: 40px; border-bottom: 1px solid #444; padding-bottom: 20px;">
        <% Object.keys(archiveData.years).sort((a, b) => a - b).forEach(function(year) { 
            var yearCount = Object.keys(archiveData.years[year]).reduce(function(total, monthKey) { return total + archiveData.years[year][monthKey].length; }, 0);
            var totalPwnCount =  new Intl.NumberFormat(["EN-US"]).format(Object.keys(archiveData.years[year]).reduce(    (total, monthKey) => total + archiveData.years[year][monthKey].reduce(        (monthTotal, item) => monthTotal + (item.PwnCount || 0), 0 ), 0)); 
            
            var predictionStr = "";
            var currentYear = new Date().getFullYear();
            if (parseInt(year) === currentYear) {
              var now = new Date();
              var startOfYear = new Date(currentYear, 0, 1);
              var daysPassed = Math.ceil((now - startOfYear) / (1000 * 60 * 60 * 24));
              if (daysPassed > 0) {
                var isLeapYear = (currentYear % 4 === 0 && currentYear % 100 !== 0) || (currentYear % 400 === 0);
                var daysInYear = isLeapYear ? 366 : 365;
                var prediction = Math.round((yearCount / daysPassed) * daysInYear);
                predictionStr = " - 🔮 prédiction : " + prediction + " fuites";
              }
            }

            var percentageStr = "";
            if (yearSurpassInfo[year] && yearSurpassInfo[year].percentage) {
              var color = yearSurpassInfo[year].diff >= 0 ? "#d9534f" : "#5cb85c";
              percentageStr = " <span style='color: " + color + "; font-weight: bold;'>(" + yearSurpassInfo[year].percentage + ")</span>";
            }
        %>
          <div style="margin-bottom: 8px;">
            <a href="#year_heading_<%= year %>" style="font-size: 1.05em; color: #e2e8f0; text-decoration: none;">
              <i class="fa fa-chevron-right me-2" style="font-size: 0.8em; color: #38bdf8;"></i>
              <strong><%= year %></strong> (<%= yearCount %>)<%- percentageStr %> avec <%= totalPwnCount %> ligne cumulée<%= predictionStr %>
            </a>
          </div>
        <% }); %>
      </div>

      <!-- Liste détaillée des fuites -->
      <div class="archive">
        <% Object.keys(archiveData.years).sort((a, b) => b - a).forEach(function(year) { %>
          <% 
          var yearCount = Object.keys(archiveData.years[year]).reduce(function(total, monthKey) { return total + archiveData.years[year][monthKey].length; }, 0);
          var totalPwnCount =  new Intl.NumberFormat(["EN-US"]).format(Object.keys(archiveData.years[year]).reduce(    (total, monthKey) => total + archiveData.years[year][monthKey].reduce(        (monthTotal, item) => monthTotal + (item.PwnCount || 0), 0 ), 0)); 
          
          var allYearItems = [];
          Object.keys(archiveData.years[year]).forEach(function(monthKey) {
            allYearItems = allYearItems.concat(archiveData.years[year][monthKey]);
          });
          allYearItems.sort((a, b) => new Date(a.BreachDate) - new Date(b.BreachDate));
          var avgTimeStr = "";
          if (allYearItems.length > 1) {
            var totalDiff = 0;
            for (var i = 1; i < allYearItems.length; i++) {
              totalDiff += new Date(allYearItems[i].BreachDate) - new Date(allYearItems[i-1].BreachDate);
            }
            var avgDiffDays = Math.round(totalDiff / (allYearItems.length - 1) / (1000 * 60 * 60 * 24));
            avgTimeStr = " <small style='font-size:0.6em; color:#999;'>&nbsp;(moyenne " + avgDiffDays + " j. entre chaque fuite)</small>";
          }

          var predictionStr = "";
          var currentYear = new Date().getFullYear();
          if (parseInt(year) === currentYear) {
            var now = new Date();
            var startOfYear = new Date(currentYear, 0, 1);
            var daysPassed = Math.ceil((now - startOfYear) / (1000 * 60 * 60 * 24));
            if (daysPassed > 0) {
              var isLeapYear = (currentYear % 4 === 0 && currentYear % 100 !== 0) || (currentYear % 400 === 0);
              var daysInYear = isLeapYear ? 366 : 365;
              var prediction = Math.round((yearCount / daysPassed) * daysInYear);
              predictionStr = " <small style='font-size:0.6em; color:#999;'>&nbsp;- 🔮 prédiction : " + prediction + " fuites</small>";
            }
          }

          var percentageStr = "";
          if (yearSurpassInfo[year] && yearSurpassInfo[year].percentage) {
            var color = yearSurpassInfo[year].diff >= 0 ? "#d9534f" : "#5cb85c";
            percentageStr = " <small style='font-size:0.6em; color: " + color + ";'>&nbsp;(" + yearSurpassInfo[year].percentage + ")</small>";
          }
          %>
          <h4 id="year_heading_<%= year %>" class="archive-ul" data-toggle="collapse" data-target="#year_<%= year %>"><%= year %> (<%= yearCount %>)<%- percentageStr %> <b class="caret"></b><%- avgTimeStr %><%- predictionStr %></h4>
          <div id="year_<%= year %>" class="collapse in">
            <h1><%= totalPwnCount %> ligne cumulée </h1>
            <% if (yearSurpassInfo[year] && yearSurpassInfo[year].date) { %>
              <p style="margin: 10px 0; padding: 10px; border: 1px solid #ddd; border-radius: 4px;">
                <strong>📊</strong> Le nombre de fuites de <%= yearSurpassInfo[year].prevYear %> (<%= yearSurpassInfo[year].prevYearCount %> fuites) a été dépassé le 
                <strong><%= yearSurpassInfo[year].date.toLocaleDateString('fr-FR', { day: 'numeric', month: 'long', year: 'numeric' }) %></strong>
                (au bout de <%= yearSurpassInfo[year].days %> jours).
              </p>
            <% } else if (yearSurpassInfo[year] && yearSurpassInfo[year].prevYearCount > 0 && yearCount < yearSurpassInfo[year].prevYearCount) { %>
              <p style="margin: 10px 0; padding: 10px; border: 1px solid #ddd; border-radius: 4px;">
                <strong>📊</strong> <%= year %> compte <%= yearCount %> fuites, soit <strong><%= (yearCount/yearSurpassInfo[year].prevYearCount*100).toFixed(1) %>%</strong> de l'activité de <%= yearSurpassInfo[year].prevYear %> (<%= yearSurpassInfo[year].prevYearCount %> fuites).
              </p>
            <% } %>
            
            <% Object.keys(archiveData.years[year]).sort((a, b) => b - a).forEach(function(month) { %>
              <% var monthCount = archiveData.years[year][month].length; %>
              <h5 class="archive-ul" data-toggle="collapse" data-target="#month_<%= year %>_<%= month %>"><%= archiveData.months[month] %> (<%= monthCount %>) <b class="caret"></b></h5>
              <div id="month_<%= year %>_<%= month %>" class="collapse in">
                <ol>
                  <% archiveData.years[year][month].forEach(function(item) { %>
                    <% 
                      var isNewToday = (new Date(item.AddedDate || item.BreachDate).toISOString().split('T')[0]) === todayStr;
                    %>
                    <li class="listing-item">
                      <span class="date_class">fuite du <%= new Date(item.BreachDate).toISOString().split('T')[0] %> ajoutée le <%= new Date(item.AddedDate||item.BreachDate).toISOString().split('T')[0] %></span> &raquo;
                      <span class="title">
                        <a href="<%- url_for('/fuites?id=' + item.index) %>" title="<%= item.Title||item.Name %>"><%= item.Title||item.Name %></a>
                        <% if (isNewToday) { %>
                          <span class="badge badge-success" style="font-size: 0.7em; background-color: #28a745; margin-left: 5px;">NOUVEAU</span>
                        <% } %>
                        <% if (item.SeverityLevel) { 
                          var severityColors = {
                            'CRITIQUE': 'danger',
                            'ÉLEVÉ': 'warning',
                            'MODÉRÉ': 'info',
                            'FAIBLE': 'secondary'
                          };
                          var severityIcons = {
                            'CRITIQUE': 'fa-exclamation-triangle',
                            'ÉLEVÉ': 'fa-exclamation-circle',
                            'MODÉRÉ': 'fa-info-circle',
                            'FAIBLE': 'fa-check-circle'
                          };
                        %>
                          <span class="badge badge-<%= severityColors[item.SeverityLevel] %> ms-2" style="font-size: 0.7em;">
                            <i class="fa <%= severityIcons[item.SeverityLevel] %>"></i> <%= item.SeverityLevel %>
                          </span>
                        <% } %>
                      </span>
                    </li>
                  <% }); %>
                </ol>
              </div>
            <% }); %>
          </div>
        <% }); %>
      </div>
    </div>

    <% if (theme.widgets.length) { %>
      <%- partial('sidebar') %>
    <% } %>
  </div>
<% } %>
